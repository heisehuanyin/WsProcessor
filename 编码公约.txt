编码公约
1.所有的实例采用两步构造法，无参构造器用于实例化一个指针，有参构造器用于生成真正的实例（在初始化过程中的所有意外都可以抛出）
2.对于对外资源采用管理器管理的方式，消除资源冲突
3.对于测试数据，放置到testdata文件夹下，不污染代码
4.对每个函数和类都尽可能的写注释，表明作用，辅助编程
5.配置文件是整个程序的核心部件，每一个窗口的构建都需要使用配置文件，构建的时候需要提供配置文件